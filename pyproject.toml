[project]
name = "expense-tracker-mcp"
version = "1.0.0"
description = "Multi-user expense, savings, budget & goal tracking MCP server"
requires-python = ">=3.10"

dependencies = [
  "fastmcp>=2.14,<3",
  "aiosqlite>=0.22"
]
